{
  "name": "freehold-demo",
  "private": true,
  "type": "module",
  "description": "Freehold public site + demos: landing (/), docs (/docs.html), passkey and data-custody demos, and the VFS self-test (/selftest.html), served over the prebuilt wasm in ./pkg.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node copy-dist-pkg.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
